/*chrome.action.onClicked.addListener(tab => {
    chrome.scripting.executeScript({
        target : {tabId : tab.id},
        func : () => {alert("Time isn't endless. Use of this to be who you want to be. Be a robotics scientist. Be a friendly person.");}
    });
});*/